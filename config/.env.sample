OPENAI_API_KEY=your-openai-api-key
PERPLEXITY_API_KEY=your-perplexity-api-key

# Full name on your Venmo account exactly as it appears in the "To" column of
# Venmo's exported CSV. Used by categorize_transactions.py to identify which
# side of each Venmo transaction is the other party.
VENMO_ACCOUNT_HOLDER_NAME=Firstname Lastname

# Google Sheets spreadsheet ID for the transaction archive
CURRENT_YEAR_ARCHIVE_SHEET_ID=your-google-sheet-id-this-year
PAST_YEAR_ARCHIVE_SHEET_ID=your-google-sheet-id-last-year

# LangSmith tracing for the LangChain agent. Optional: if LANGSMITH_API_KEY is
# unset, tracing silently no-ops and the pipeline runs normally.
LANGSMITH_TRACING=true
LANGSMITH_API_KEY=your-langsmith-api-key
LANGSMITH_PROJECT=agentic-transaction-categorizer

# Browser automation for downloading Chase transactions. The URL should point
# at the desired card/account; the persistent local browser profile remembers
# device trust between runs. BROWSER_AUTOMATION_MODEL is optional.
CHASE_URL=https://secure.chase.com/web/auth/dashboard#/dashboard/summary/your-account-id/CARD/BAC
CHASE_USERNAME=your-chase-username
CHASE_PASSWORD=your-chase-password
# Optional: set this only if Chase requires choosing among multiple devices.
CHASE_MFA_DEVICE=your-device-name-as-shown-by-chase
BROWSER_AUTOMATION_MODEL=gpt-5.6-luna
